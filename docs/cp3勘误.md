把整个banner完全去掉，我认为还是不太行，就是原生感受到了严重影响

然后终端状态的状态点我认为应该是由输出决定，而不是收到输入的影响，这个东西本质上是让你知道终端还在不在运行的

但是我不确定好不好实现，要是不好实现，按照现在的实现其实也可以

然后就是对于claudecode这种，刚刚点进去的时候会有一个状态变化，我其实不太清楚是不是因为触发了重绘，导致瞬间跳到绿色点，然后恢复黄色点，我认为这个噪声至少需要过滤掉

在收藏路径上右键没有反应

现在标签顺序还是有bug，有的时候，一个顺序还是会因为另外一个的操作而变化，我们现在先彻底移除灰色标签页在最右边的功能，而是每个窗口自己管理标签页顺序，然后现在既然还没有实现拖动，标签页顺序就不在应该以任何形式移动。另外目前侧边栏顺序和标签页顺序还是有一点张力的，删除相关功能后，应该保持同步

